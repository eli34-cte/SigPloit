tasaklar
hexeditor dosya_adi
strings dosya_adi | grep "http"
strings: Warning: 'CraxsRatV7.4' is a directory
echo "export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64" >> ~/.bashrc
echo "export PATH=\$JAVA_HOME/bin:\$PATH" >> ~/.bashrc
source ~/.bashrc
export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
export PATH=$JAVA_HOME/bin:$PATH
git clone https://github.com/SigPloiter/SigPloit.git
cd SigPloit
sudo apt update && sudo apt upgrade -y
sudo apt install git python3 python3-pip openjdk-11-jdk -y
sudo apt update && sudo apt upgrade -y
sudo apt install python2 lksctp-tools -y
sudo apt install python2
2️⃣ Pip2 Kurulumu
Kali’de bazen pip2 bulunmaz, aşağıdaki komutlarla yükleyebilirsin:

curl https://bootstrap.pypa.io/pip/2.7/get-pip.py -o get-pip.py
python2 get-pip.py
3️⃣ SigPloit'i İndir
GitHub'dan SigPloit'i indir:

git clone https://github.com/SigPloiter/SigPloit.git
cd SigPloit
4️⃣ Gerekli Kütüphaneleri Kur
Şimdi SigPloit için gerekli Python kütüphanelerini yükleyelim:

sudo pip2 install -r requirements.txt
5️⃣ SigPloit'i Çalıştır
Şimdi SigPloit’i çalıştırabilirsin:

python2 sigploit.py
udo apt install python2 curl -y
curl https://bootstrap.pypa.io/pip/2.7/get-pip.py -o get-pip.py
python2 get-pip.py
3️⃣ pip2’nin başarıyla yüklendiğini kontrol et:

bash
Copy
Edit
pip2 --version
Eğer versiyon numarası çıkıyorsa sorun çözülmüştür ✅

🚀 SigPloit'i Tekrar Kur ve Çalıştır
Şimdi SigPloit için gerekli bağımlılıkları tekrar yükle:

bash
Copy
Edit
cd SigPloit
pip2 install -r requirements.txt
python2 sigploit.py
⚠️ Hala Çalışmıyorsa?
Eğer hala "pip2 bulunamadı" hatası alıyorsan:

bash
Copy
Edit
sudo ln -s /usr/local/bin/pip2 /usr/bin/pip2
Sonra tekrar çalıştır:

bash
Copy
Edit
pip2 --version
